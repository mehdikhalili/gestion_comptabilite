{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <div class="row container">
        <button class="btn btn-primary mt-3 mb-1"><i class="fas fa-check"></i> {{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
