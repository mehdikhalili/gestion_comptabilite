<nav class="navbar navbar-expand-lg navbar-dark bg-info mb-4">
  <div class="container">
    <a class="navbar-brand" href="{{ path('home') }}">
      <img class="img-fluid" src="/images/logo.jpg"  style="width: 140px;">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample07" aria-controls="navbarsExample07" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarsExample07">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('transaction_index') }}">Table de suivie</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('categorisation_index') }}">Gestion des catégories</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('bank_account_index') }}">Gestion des banques</a>
        </li>
        <li class="nav-item border-left">
          <div class="btn-group">
            <a class="nav-link">{{ app.user.username }}</a>
            <a class="nav-link btn rounded-0 dropdown-toggle dropdown-toggle-split" data-toggle="dropdown"></a>
            <div class="dropdown-menu dropdown-menu-right">
              
              {% if is_granted('ROLE_DIRECTEUR') %}
                <a class="dropdown-item" href="{{ path('user_edit', {type : 'directeur'}) }}">Modifier votre infos personnelles</a>
                <a class="dropdown-item" href="{{ path('user_edit', {type : 'comptable'}) }}">Modifier le comptable</a>
              {% endif %}

              {% if is_granted('ROLE_COMPTABLE') %}
                <a class="dropdown-item" href="{{ path('user_edit', {type : 'comptable'}) }}">Modifier votre infos personnelles</a>
              {% endif %}
              
              <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
            </div>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>