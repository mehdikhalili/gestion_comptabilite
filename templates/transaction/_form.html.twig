{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mb-1"><i class="fas fa-check"></i> {{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
