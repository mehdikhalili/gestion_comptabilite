{% extends 'base.html.twig' %}

{% block title %}Edit Transaction{% endblock %}

{% block body %}
    <h1 class="mb-4">Modifier une transaction</h1>

    {{ include('transaction/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    {{ include('transaction/_delete_form.html.twig') }}

    <div id="currentDebit" hidden>{{ transaction.debit }}</div>
    <div id="currentCredit" hidden>{{ transaction.credit }}</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('transaction') }}
{% endblock %}
